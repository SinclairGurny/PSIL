/** 
    psil_eval.cpp
    PSIL Semantic Evaluation Implementation
    @author Sinclair Gurny
    @version 0.1
    July 2019
 */

#include "psil_eval.h"

std::unique_ptr<psil_parser::token_t>
psil_eval::token_tree_copy( const std::unique_ptr<psil_parser::token_t> & root ) {
  return nullptr;
}
  
bool psil_eval::check( const std::unique_ptr<psil_parser::token_t> & tk ) {
  return false;
}
